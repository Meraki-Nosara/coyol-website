# Memory — July 13, 2026

## Morning Session (Greece timezone)

### 💸 API Cost Issue Discovered
- Marion noticed $10 spent overnight on Claude API
- **Root cause:** Stuck heartbeat session (2d4c438b) kept retrying every 30-40 minutes all night
- Each retry loaded full context (~200K tokens on Opus) = expensive
- Session was trying to recover an incomplete "thinking" response
- **Fixed:** Killed stuck session, gateway restarted
- **Action:** Heartbeats disabled while Marion traveling (Greece until September)

### 🤖 v2.2 Reservation AI Support Widget
**Major feature deployed!** Added AI chat widget to La Luna and Coyol reservation admin pages.

**Purpose:** Staff support — so hostess/managers can ask questions about the system instead of messaging Marion on WhatsApp.

**Implementation:**
- ChatWidget.astro component with floating chat bubble
- API endpoint at /api/chat-support.ts
- Uses OpenRouter with Gemini Flash 2.0 (cost-effective)
- Spanish language support for staff
- System prompt includes full knowledge of reservation system features

**Key learnings:**
1. Marion's Anthropic API key didn't work (model not found errors)
2. Switched to OpenRouter — needed new env var `OPENROUTER_API_KEY` in Vercel
3. Had CSS issues — text was invisible (black on black), fixed with explicit colors + bubble styling
4. Had ID conflicts between `#chatInput` and `#aiChatInput` — fixed selectors

**Project clarifications:**
- "NC Control" was wrong — there's no NC Control
- The reservation system lives inside **coyol-website** codebase:
  - `/laluna/admin/` — La Luna reservations
  - `/restaurant/admin/` — Coyol reservations
- Meraki Control = financials dashboard (separate)
- Nosara Construction = materials ordering (different business entirely)

**Deployed to:**
- https://coyolnosara.com/laluna/admin (v2.2)
- https://coyolnosara.com/restaurant/admin (v2.2)

### 📝 Environment Variables on Vercel (coyol-nosara)
- `ANTHROPIC_API_KEY` — added but doesn't work (model access issue)
- `OPENROUTER_API_KEY` — added and working (Gemini Flash)
