# Memory — August 25, 2026

## Fixes Deployed Tonight (1 AM Greece time)

### 1. Auto-refresh Reservations (La Luna + Coyol)
- **Issue:** Julia had to close/reopen app to see updated reservations
- **Fix:** Added 30-second auto-refresh interval
- Only refreshes when tab is active and online
- Files: `src/pages/laluna/admin/index.astro`, `src/pages/restaurant/admin/index.astro`

### 2. Coyol Gift Card Redemption
- **Issue:** Applying gift cards in Coyol didn't work (balance stayed same)
- **Root cause:** `coyol_gift_cards` table missing `notes` column that La Luna has
- **Fix:** 
  - Updated `/api/gift-cards` to auto-strip `notes` for Coyol cards
  - Made Coyol use same API endpoint as La Luna
- Files: `src/pages/api/gift-cards.ts`, `src/pages/restaurant/admin/gifts.astro`

### Database Schema Difference
- `laluna_gift_cards` has: notes, applied_amount, applied_at, applied_to_reservation, sent_at
- `coyol_gift_cards` missing these columns — should add them for consistency

### Deployment Note
- GitHub → Vercel auto-deploy still not working
- Must use `npx vercel --prod` from coyol-website folder
