# Memory — May 19, 2026

## La Luna Reservation System Setup (Morning Session)

### Progress
- Reviewed existing La Luna website (`laluna-website/`)
- Found reservation form was using localStorage, not Supabase
- Created `scripts/send-confirmation.cjs` for La Luna (terracotta branding)
- Created `scripts/check-reservations.cjs` for La Luna

### Email Setup ✅
- **Resend domain verified:** `lalunanosara.com` 
- DNS records added to GoDaddy:
  - DKIM: `resend._domainkey` TXT record
  - MX: `send` pointing to `feedback-smtp.us-east-1.amazonses.com`
  - SPF: `send` TXT record with `v=spf1 include:amazonses.com ~all`
- Marion is on Resend **Transactional Pro** ($20/mo) — 50k emails/month
- Same Resend API key works for both Coyol and La Luna: `re_EJWDiPdh_DJZQhsSJUzyNwpLdzAfoVdgW`

### Supabase Edge Function ✅
- Created `laluna-reservation` edge function via Supabase dashboard editor
- Function sends confirmation email via Resend when new reservation inserted
- **Next step:** Add RESEND_API_KEY secret to Supabase, then set up database trigger

### Architecture (Both Systems Independent)
| | Coyol | La Luna |
|---|---|---|
| Table | `coyol_reservations` | `laluna_reservations` |
| Edge Function | `coyol-reservation` | `laluna-reservation` |
| Email Domain | `coyolrestaurant.com` | `lalunanosara.com` |
| From Address | `reservations@coyolrestaurant.com` | `reservations@lalunanosara.com` |

### Still TODO
- [ ] Add RESEND_API_KEY secret to Supabase
- [ ] Create database trigger to call `laluna-reservation` on INSERT
- [ ] Update admin page with La Luna logo (top left)
- [ ] Match table arrangement to EatApp layout (for crew familiarity)
- [ ] Deploy La Luna website to Vercel
- [ ] Test end-to-end reservation flow
